using System;

namespace ConsoleAppTestConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            //definition de variable int
            int a; //nombre a pour les moyennes
            int b; //nombre b pour les moyenne
            int r; // nombre pour rayon
            int i; //interet
            int n; //nombre d'annee
            int degres; //nombre pour degres a la place de a
            int moyenne; // moyenne
            

            //definition de variable double
            double air; //air
            double pi = Math.PI; //def de pi
            double s; // euro placer sur un compte
            double volume; // le volume de r
            double rcarre = Math.Pow(r, 2); //le carre de r
            double rcube = Math.Pow(r, 3); //le cube de r
            double rsecteur; // aire du secteur circulaire
            double isimple; // interet simple
            double icompose; // interet compose
            double pcomp; // pour calculer interet compose
            
            //saisie




            //traitement-calcul

            moyenne = (a + b) / 2; // calcul de moyenne a et b

            //air = 4 * pi * rcarre; // calcul l'air d'une sphere

            //volume = 4 / 3 * pi * rcube; // calcul le volume de la sphere

            //rsecteur = pi * rcarre * a / 360; //calcul de l'air du secteur circulaire

            //isimple = s * (1 + n * i); // calcul interet simple

            //pcomp = i + 1; // calcul de la puissance de l'interet compose

            //icompose = s * Math.Pow(pcomp, n); // calcul de l'interet compose

            




            //affichage


            Console.WriteLine("Ex 1 : Calcul de moyenne ");
            Console.WriteLine(" ");
            Console.WriteLine("Entre un nombre pour a : ");
            a = Console.ReadLine();
            Console.WriteLine("Entre un nombre pour a : ");

        }
    }
}
